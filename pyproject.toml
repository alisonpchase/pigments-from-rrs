[tool.black]
line-length = 100

[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm[toml]>=6.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "pigments_from_rrs/_version.py"
write_to_template = "__version__ = '{version}'"